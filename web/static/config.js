// API origin for /api/... when the UI is on a different host (e.g. Netlify).
// Netlify build overwrites this from KEEP_SWINGING_API_BASE; leave empty for same-origin (Go server).
window.__KEEP_SWINGING_API_BASE__ = '';
